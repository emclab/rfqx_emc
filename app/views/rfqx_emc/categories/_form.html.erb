<%= simple_form_for @category do |f| %>    
 
  <%= f.input :name, :label => "名称 ：" %>  
  <%= f.input :description, :label => "内容 ：" %> 
  <%= f.input :active, :label => "有效? ：", :as => :radio %> 
  <%= f.button :submit %>  
<% end %> 


